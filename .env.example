# Database connection string
# For local development, run `npx prisma dev` to start a local PostgreSQL
# For production, use a cloud PostgreSQL provider (Supabase, Railway, etc.)
DATABASE_URL="postgresql://user:password@localhost:5432/racktar?schema=public"

# NextAuth.js configuration
NEXTAUTH_SECRET="your-secret-key-here"
NEXTAUTH_URL="http://localhost:3000"
